#include <stdio.h>

int main(void)
{

    int a = 6, b = 2;

    printf("++a = %d\n", ++a);    // Raises the value of an operand by one
    printf("--b = %d\n", --b);    // Reduces the value of an operand by one

    return 0;
}